rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read provider profile images
    // Only authenticated admin users can upload, replace, or delete images
    match /providers/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
